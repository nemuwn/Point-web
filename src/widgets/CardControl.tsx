import { PhonePage } from "@/components/PhonePage";
import { Phone3 } from "@/components/Logo";
export const CardControl = () => {
  return (
    <PhonePage
      title="SECURITY"
      header="Card Control"
      Img={Phone3}
      Description="Lock your card on the fly or get notified the instant you're charged — Point app is always there to ensure that you have complete control over when, where, and how your PointCard is used."
    />
  );
};
