import { PhonePage } from "@/components/PhonePage";
import { Phone1 } from "@/components/Logo";
export const InstantFeed = () => {
  return (
    <PhonePage
      title="POINT APP"
      header="Instant Transfer"
      Img={Phone1}
      Description="Still waiting ‘3 to 5 business days’ to move funds? We made it instant — as in right now. Use any other debit card to fund your Point account in seconds."
    />
  );
};
